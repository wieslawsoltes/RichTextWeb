# Word feature audit and implementation plan

Audit date: 2026-09-19; resumed and updated 2026-09-20. Baseline: `7f6242b6355b7448bf975a96f12aa18a161ea9a7` (0.5.0). This audit covers the core model/engine, every top-level source module, reusable controls, adapters, formats, tests and Document Studio. It is a capability and qualification inventory, **not a claim of complete Microsoft Word parity**. A preserved DOCX element is not necessarily editable or rendered faithfully; an exposed command is not evidence of complete Word semantics.

The authoring increment adds table formulas and sorting, repeated-header authoring, captions and cross-references, a caption index, expanded fields, native document properties/variables, and reusable dialogs/keyboard commands. It also fixes field cache loss, offsets around embedded objects, caret remapping, inherited field metadata during typing and caption continuation. All document mutations go through the reusable engine and history; the sample is a client, not a second implementation.

## Existing and extended capability inventory

Paths in the implementation column are under `src/`; named test files are under `tests/`. Every row remains subject to the documented boundary. The inventory deliberately avoids a percentage: there is no versioned, exhaustive Word conformance corpus in this repository.

<!-- prettier-ignore -->
| Area | Implemented behavior and owning modules | Evidence and remaining boundary |
| --- | --- | --- |
| Flow tree and ownership | `model.ts`: owned FlowDocument, Section, paragraphs, inline wrappers, runs, lists, table groups/rows/cells/columns, images, equations and embedded/floating nodes; canonical JSON | `model.test.ts`, `registry.test.ts`. Not the complete native WPF object/API/visual-tree model. |
| Dependency properties and styles | `model.ts`: metadata, owners, read-only keys, validation/coercion, inheritance, current/local values, value-source inspection, styles and triggers | `property-system.test.ts`. Native expression/animation precedence and every Word theme/style inheritance rule are not implemented. A property bag alone does not establish layout support. |
| Positions and selections | `model.ts`, `engine-tree.ts`, `engine.ts`: live/snapshot UTF-16 positions, gravity, grapheme navigation, structural-symbol conversion, rich ranges and insertion formatting | `engine.test.ts`, `model.test.ts`. UTF-16 editing coordinates are not native WPF symbol offsets. Browser/OS-specific bidirectional and IME behavior still needs broader qualification. |
| Editing and commands | `engine.ts`, `engine-tree.ts`: insert/delete/replace, paragraph splitting/merging, word operations, rich fragments, hyperlinks, images, equations, lists, tables, block moves and command dispatch | `engine.test.ts`, `control.browser.mjs`. No universal Word editing-intent equivalence for arbitrary overlapping structural selections. |
| Undo and performance | `history.ts`, `engine.ts`: grouped changes, reversible compact patches, stable object identity, direct eligible Run typing | `engine.test.ts`, `review.test.ts`; `scripts/benchmark.mjs`. Structural mutations/index maintenance may scale with document size; not a persistent rope or universal constant-time editor. |
| Character/paragraph formatting | `engine.ts`, `toolbar.ts`, `control-renderer.ts`: font, color, emphasis, size, alignment, paragraph properties, indentation, lists, clearing formatting | `toolbar.browser.mjs`, `engine.test.ts`. Exhaustive typography, OpenType controls, advanced multilevel numbering and complete theme/style management remain. |
| Table structure | `engine.ts`, `table-grid.ts`: validated logical grid, rows/columns, cell merge/split, rich content, table/cell properties; shared span validation | `engine.test.ts`, `review.test.ts`, `word-authoring.test.ts`. Complete Word AutoFit/layout, all merge/split selection semantics and nested page fragmentation remain unqualified. |
| Table data tools (new) | `formula.ts`, `table-formulas.ts`, `table-grid.ts`: bounded arithmetic, 18 functions, cell/range/directional references, numeric field dependencies, stable three-key sorting, leading-header authoring | `word-authoring.test.ts`, `word-authoring.browser.mjs`. Sorting stays within original row groups, rejects vertical merges and uses strict numeric/ISO date input. Formulas do not implement every Word grammar/locale/external reference. |
| Continuous renderer | `control-renderer.ts`, `virtualization.ts`: keyed retained DOM, browser shaping/bidi, top-level block windowing, distant selection materialization and composition protection | `control.browser.mjs`, `layout.browser.mjs`. A single huge section/table is not subdivided; total model traversal is not eliminated. |
| Finite layout | `pagination.ts`, `floating-layout.ts`, `page-window.ts`, `page-setup.ts`: measured pages/columns, break/keep/widow behavior, page slots, cached geometry, visible-first realization, page numbering and diagnostics | `layout.test.ts`, `page-authoring.test.ts`, browser counterparts. Full body measurement remains; interacting anchors, footnote balancing and section-dependent page geometry are not Word-identical. |
| Views/navigation/print | `control.ts`: PrintLayout, WebLayout, ReadMode, Outline, Draft; single/two/vertical/multiple pages, fitting, zoom, page navigation, selection-preserving activation and measured-page printing | `layout.browser.mjs`, `authoring.browser.mjs`, `page-authoring.browser.mjs`. One live editing surface with page mirrors; not Word master-document outlining or certified print fidelity. |
| Headers/footers/stories | `story-session.ts`, `document-features.ts`, `control.ts`: rich scoped drafts, default/first/even stories, cancel, conflict detection, parent undo and double-click editing | `page-authoring.test.ts`, `page-authoring.browser.mjs`. Full section link-to-previous rules and every Word story/anchor interaction remain. |
| Notes | `document-features.ts`, `pagination.ts`, `formats-docx.ts`: insert/update footnotes/endnotes, references, native note parts, rendering | `document-features.test.ts`, `docx-advanced.test.ts`. Complete section restart rules, continuation separators and Word per-page balancing remain. |
| Floating objects | `floating-layout.ts`, `floating-adorner.ts`, `control.ts`: Figure/Floater rich stories, positioned/wrapped images and text boxes, move/resize and story editing | `floating-formats.test.ts`, `pdf-floating.test.ts`, browser layout/authoring tests. Complex nested exclusions, overlapping anchor constraints and all DrawingML shape/effect semantics remain. |
| Fields and automation (extended) | `field-code.ts`, `field-references.ts`, `document-features.ts`: PAGE/NUMPAGES, REF/PAGEREF, DATE/TIME, TITLE/AUTHOR/FILENAME, MERGEFIELD, SEQ; new arithmetic/IF, DOCPROPERTY/DOCVARIABLE, NUMWORDS/NUMCHARS/NUMPARAS, SECTION/SECTIONPAGES, CREATEDATE/SAVEDATE; locking, unlinking, diagnostics | `document-features.test.ts`, `word-authoring.test.ts`. Native instructions/caches are retained. Complete nested field grammar, all switches/pictures and automatic dependency/layout scheduling are not implemented. |
| Captions/references/indexes (new) | `document-features.ts`: independent caption paragraphs, label-scoped sequences, whole/number/label-number bookmark targets, REF/PAGEREF hyperlinks and label-filtered table of figures/tables | `word-authoring.test.ts`, native DOCX assertions, browser dialogs. Chapter numbering, automatic caption insertion and arbitrary Word table-of-authorities/index/bibliography fields remain. |
| Contents and mail merge | `document-features.ts`: heading-based TOC, page-reference updates and data-supplied merge fields | `document-features.test.ts`, `docx-advanced.test.ts`. Full directory/label/envelope merge, recipient queries, conditional sections and bibliography/style databases remain. |
| Statistics (new) | `document-statistics.ts`: Unicode word segmentation, code-point characters with/without spaces, paragraph/logical-line counts, selection range and optional notes | `word-authoring.test.ts`, read-only browser dialog. Not Word proofing-engine counts or typeset line statistics; headers and floating stories are not implicitly counted. |
| Review/comments/bookmarks | `engine.ts`, `history.ts`, `formats-docx.ts`: text/format/move and table/list/fragment structural revisions, conflict-aware accept/reject, atomic reject-all, comments and named bookmarks | `review.test.ts`, `review-docx.test.ts`, `review.browser.mjs`. Exhaustive overlapping/nested revisions and identity services remain; inverse changes can conflict with later edits. |
| Coauthoring | `collaboration.ts`, `collaboration-document.ts`: protocol-2 rich transactions, per-Run character merge, deterministic structural moves, causal queues, snapshots and acknowledged checkpoint epochs | Collaboration Node/browser suites. Authentication, persistence, membership, production transport, reconnect policy and trustworthy checkpoint authority are host responsibilities. Structural splits do not preserve every Word coauthoring intent. |
| Equation model/rendering | `equations.ts`, `equation-structure.ts`, `equation-control.ts`: editable LaTeX/presentation MathML, bounded private MathJax SVG, 42 templates, token/fraction/root/script edits, matrix rows/columns, alternate text and undo | Equation and authoring suites. Restricted TeX/MathML, rectangular non-spanning matrices up to 32 by 32; not all UnicodeMath or Word semantic math structures. |
| Equation interchange | `equations-omml.ts`, `equations-pdf.ts`: supported presentation MathML to native Office Math and vector PDF; retained exact equation source where valid | `equations.test.ts`, `equation-format-control.test.ts`. Unsupported conversions fail rather than silently reinterpret; advanced spacing/layout and tagged/searchable math remain. |
| DOCX | `formats-docx.ts`: supported styles/numbering, stories, notes, fields, TOC, comments/bookmarks/review, anchored content and equations; new native locks, formula instructions, caption styles/index, repeated headers, core/custom properties and docVars | Format/review/word-authoring suites inspect native XML and import without RichTextWeb extension metadata. No universal lossless round trip; private extensions preserve some richer state and can be invalidated by external edits. |
| Text/HTML/Markdown/XAML | `formats.ts`, `formats-markup.ts`: explicit structured conversion, safe markup and supported rich/math/floating metadata | `formats.test.ts`, `floating-formats.test.ts`. Arbitrary HTML/CSS/JavaScript equivalence and exact native XAML runtime equivalence are not claimed. |
| PDF export/import | `formats-pdf.ts`, `pdf.ts`, `pdf-import.ts`, `pdf-operators.ts`: independent flow PDF generation, PDF.js preview/search/reconstruction, original supported Tj/TJ/quote operator edits, encoding/advance preservation and shared-Form isolation | PDF suites. Not arbitrary semantic paragraph reflow, OCR, secure redaction, PDF/A/PDF-UA certification or Word-identical pagination. |
| PDF controls | `pdf-control.ts`: page organization, overlays, source edits, search/reconstruction and undo via a reusable control | `pdf.browser.mjs`, `pdf.test.ts`. Production print/color/signature/accessibility certification remains separate. |
| Reusable editor controls | `control.ts`, `toolbar.ts`, `web.ts`: rich editor/page editor/viewers, validated command dialogs, document/selection lifetimes, read-only guards; new formula/sort/caption/reference/field/statistics dialogs and shortcuts | Control/toolbar/authoring suites. Firefox/WebKit, physical touch/IME and assistive-technology qualification are separate from Chromium checks. |
| React/MVVM/bridge | `react.ts`, `mvvm.ts`, `bridge.ts`: React lifecycle/ref hooks, .NET-style observable view models, validated WebView protocol and engine reuse | `adapters.test.ts`, `bridge.test.ts`, `react.browser.mjs`. Adapters do not replace the engine with a native implementation. |
| Public entry points | `core.ts`, `index.ts`, `web.ts`, package exports: DOM-independent engine/API modules, ESM/CJS/types and standalone bundles | Installed-package tests. All declarations are not promises of Word or WPF binary compatibility. |
| Native/Blazor hosts | `adapters/`, `blazor/`: WPF, WinUI, Avalonia and Blazor WebAssembly/Server hosts, shared protocol, assets, input/forms and binding APIs | Native workflow and .NET 8/10 package consumers. Windows smoke does not prove non-Windows Avalonia or physical-device coverage. New field helpers are available through existing engine/interop APIs; not every helper has a dedicated typed C# wrapper. |
| Document Studio | `sample/`: RibbonWeb, Dockyard, TreeDataGridWeb, DynamicDataWeb, ReactiveWeb, RBushWeb and QuikGraphWeb integration; five views, source editing, properties, review, navigation and sample templates | `workspace.browser.mjs`; new automation template and References/Table layout actions in `word-authoring.browser.mjs`. Sample ecosystem libraries are not core runtime dependencies. |
| Distribution | `scripts/`, `.github/workflows/`: npm and native archives, isolated consumers, checksums, release gates and exact-commit Pages smoke | CI artifacts and `docs/VERIFICATION.md`. Build/test results belong to their checked commit; a source commit is not an npm or NuGet release. |

## New authoring contract

See [WORD-AUTHORING.md](WORD-AUTHORING.md) for runnable examples and command names. The new standalone helpers are exported from the normal/core entry points. The table formula resolver is internal coordination; applications use `DocumentFeatures` or `evaluateFormula` with an explicit reference callback.

A formula is parsed, never executed as JavaScript. Supported functions are `ABS`, `AND`, `AVERAGE`, `COUNT`, `DEFINED`, `FALSE`, `IF`, `INT`, `MAX`, `MIN`, `MOD`, `NOT`, `OR`, `PRODUCT`, `ROUND`, `SIGN`, `SUM`, `TRUE`. Parsing is bounded to 8,192 characters, 2,048 tokens and 64 nested parse levels; evaluation has an 8,192-step budget and 10,000 referenced/aggregate-value caps. Table grids have a 10,000-cell cap. Field instructions, date/number pictures and dependency recursion are separately bounded.

A1 and RnCn references resolve in the nearest containing table. Ranges and directional references deduplicate merged cells and exclude the formula cell; directional references exclude marked header rows. Bare `R`/`C` refer to the current row/column; an A1-looking name takes A1 precedence. Numeric dependencies resolve before formatting. Cross-table bookmarked ranges, general external data and the full Word numeric/date/locale grammar are not implemented. Date pictures use UTC; unsupported instructions preserve cached display and return a reason instead of silently evaluating to a fabricated result.

Sorting preserves row/cell/node IDs and rich formatting, keeps the caret with the original paragraph and uses the existing history/review machinery. It sorts within existing body row groups to retain group inheritance, supports one to three keys and rejects vertical merges. Date keys require ISO dates or timezone-qualified timestamps. Repeated-header authoring partitions real header/body groups for DOM `thead` rendering and emits native `w:tblHeader`; it does not establish Word-identical repeated-header pagination.

Caption creation, its three bookmark targets and sequence update are one undo action. Caption metadata does not leak into a newly typed following paragraph. Headless reference resolution defaults to the pre-update source snapshot. `ReferenceMode: "Current"` resolves supported REF chains and bookmarked formula operands against pending results in one pass; reusable Update fields/F9 commands use this mode. It handles cycles, locked caches, original-offset remapping and bounded expansion. General nested instruction evaluation, persistent incremental scheduling, contents rebuilding and layout/TOC fixed points remain outstanding. Page/section fields need valid layout context; the page control supplies measured page information only for its current document revision.

## Prioritized remaining implementation work

<!-- prettier-ignore -->
| Priority | Workstream | Concrete completion/acceptance gate |
| --- | --- | --- |
| P0 | Native interoperability corpus | Versioned DOCX fixtures without private metadata; import/edit/export and Word open/save/reopen comparisons covering every implemented feature. Publish exact losses and render diffs. |
| P0 | Incremental typesetting | Fragment long tables/sections, invalidate dependent regions, resolve anchor/footnote/column interactions and section page geometry. Verify bounded realized content, large-document edit latency and stable selections without complete-body remeasurement. |
| P0 | Field dependency scheduler | Stable field graph across stories, nested instructions, cycle diagnostics, locked dependencies, sequence/reference/TOC/page fixed points and incremental invalidation. Updating a caption must update references/index/page caches deterministically in one bounded operation. |
| P1 | Styles, themes and lists | Named character/paragraph/table/list styles, based-on/next/link inheritance, theme font/color maps, multilevel numbering definitions/restarts and full management controls. Native DOCX fixtures must survive repeated edits. |
| P1 | Forms and content controls | Structured document tags, text/date/choice/checkbox/repeating controls, placeholders, validation, data binding and protected editable regions. Define security and history/collaboration semantics before exposing controls. |
| P1 | References and long documents | Chapter captions, indexes, authorities, citations/bibliography styles, complete note numbering/continuation and advanced TOC switches. Add editable reusable panels, not just export metadata. |
| P1 | Drawing and media | Shape/path model, connectors, grouping, DrawingML transforms, image crop/effects, charts and diagram authoring; deterministic wrap/anchor integration and native interchange. |
| P1 | Review and document comparison | General overlapping/nested changes, compare/combine documents, authors/permissions and conflict UX. Prove preservation under simultaneous editing and selective rejection. |
| P1 | Production collaboration | Authenticated service adapter, durable revisions, reconnect/offline persistence, causal recovery, membership and checkpoint authority. Exercise dropped/reordered messages, recovery and scale with failure injection. |
| P2 | Proofing and multilingual authoring | Pluggable dictionaries/grammar/hyphenation, language runs, suggestions, autocorrection, accessibility checks and explicit privacy controls for external services. Physical IME/screen-reader qualification is required. |
| P2 | Advanced mail merge and print products | Recipient management/filtering, conditional sections, directories, labels/envelopes and batch/export workflow. Reject unauthorized external data access; test print geometry. |
| P2 | Platform and output qualification | Firefox/WebKit, touch/pen, macOS/Linux Avalonia, physical assistive technology; tagged PDF, color/font/print production and digital-signature requirements. Do not infer these from a Chromium smoke test. |

VBA, ActiveX, active OLE, arbitrary external field execution and Office add-in binary compatibility are not safe default browser features. A future trusted host integration must be an explicit opt-in capability with a separate threat model, permission boundary and tests. This is distinct from safely preserving inert unsupported content.

## Evidence and maintenance

The baseline passed 405 Node tests. The authoring increment plus the continuation adds 105 regressions (510 total) covering formula parsing and bounds, dependency/cycle handling, rich sort identity, header groups, field cache/offset/history fixes, captions/references, Unicode statistics and native DOCX import/export. The continuation adds nine leading-boundary/history regressions and twenty-two dependency/caption/native-DOCX tests, and eight conditional-bookmark/optional-false/native-DOCX tests. PR #22 passed Node 22/24 Chromium, package consumers, Windows desktop and Blazor qualification at `a42cf88`; PR #23 passed 502 Node tests and 123 Chromium groups plus native and Blazor checks at `8ad5eaa`; subsequent conditional-field changes require their own checked-commit results. Browser coverage is in `word-authoring.browser.mjs`, integrated into the existing full suite; consult the checked commit's CI artifact for the executed result rather than treating test presence as a pass.

Reproduce with `npm run typecheck`, `npm run check`, `npx playwright install --with-deps chromium`, and `npm run test:browser`. The implementation environment blocks browser navigation; browser/package-consumer results are obtained from GitHub Actions. No Microsoft Word desktop visual comparison was performed for this increment.

The current inventory contains 42 top-level source modules. Generate a machine-readable declaration inventory with `node scripts/audit-word-features.mjs > feature-inventory.json`. It lists all top-level source files, declared exports/public class members and test files; it intentionally does not infer behavioral coverage from matching names. Keep this audit and the compatibility matrix current when completing a workstream, and attach fixtures plus checked-commit evidence before changing a boundary to qualified.

### Primary Word behavior references

- Microsoft Support: [Use a formula in a Word table](https://support.microsoft.com/en-us/word/use-a-formula-in-a-word-table).
- Microsoft Support: [List of field codes in Word](https://support.microsoft.com/en-us/word/list-of-field-codes-in-word).
- Microsoft Support: [Create a cross-reference](https://support.microsoft.com/en-us/word/create-a-cross-reference).
- Microsoft Support: [Add, format, or delete captions](https://support.microsoft.com/en-us/word/add-format-or-delete-captions-in-word).
- Microsoft Support: [Sort the contents of a table](https://support.microsoft.com/en-us/word/sort-the-contents-of-a-table).
- Microsoft Support: [Update fields](https://support.microsoft.com/en-us/word/update-fields).

These define comparison targets, not a certification of this implementation. Related repository contracts: [compatibility](COMPATIBILITY.md), [engine/history](ENGINE.md), [page authoring](PAGE-AUTHORING.md), [equations](AUTHORING.md), [formats](FORMATS.md), [collaboration](COLLABORATION.md), [PDF](PDF.md), [integration](INTEGRATION.md) and [verification](VERIFICATION.md).
